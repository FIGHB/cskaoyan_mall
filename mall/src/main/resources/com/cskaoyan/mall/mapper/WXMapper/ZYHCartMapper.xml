<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cskaoyan.mall.mapper.WXMapper.ZYHCartMapper">
  <update id="updateCartChecked">
    update cskaoyan_mall_cart
    set  checked = #{isChecked}
    where product_id = #{productId}
  </update>
  <select id="queryCartListByUserId" resultType="com.cskaoyan.mall.bean.Cart">
    select id, user_id as userId, goods_id as goodsId, goods_sn as goodsSn, goods_name as goodsName, product_id as productId,
    price, number, specifications,checked, pic_url as picUrl, add_time as addTime, update_time as updateTime, deleted
    from cskaoyan_mall_cart
    where user_id = #{userId}
  </select>

</mapper>